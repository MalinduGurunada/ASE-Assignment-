# ── Local PostgreSQL ──────────────────────────────────────
RMT_DB_URL=jdbc:postgresql://localhost:5432/rmt
RMT_DB_USERNAME=postgres
RMT_DB_PASSWORD=change_me
RMT_JWT_SECRET=replace_with_base64_secret_at_least_256_bits

# ── Supabase (fill from Project Settings → Database) ──────
# RMT_DB_URL=jdbc:postgresql://db.<project-ref>.supabase.co:5432/postgres?sslmode=require
# RMT_DB_USERNAME=postgres
# RMT_DB_PASSWORD=<your-supabase-db-password>
# RMT_JWT_SECRET=<your-jwt-secret>
